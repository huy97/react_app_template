import React from "react";

import { StyledLoading, StyledSpinner } from "./style";

function Loading() {
  return (
    <StyledLoading>
      <StyledSpinner />
    </StyledLoading>
  );
}

export default Loading;
